.container
  .card
    .card-header
      h1
        = t('.title')
    .card-text
      .card-block
        = render 'form', train: @train
    .card-footer
      = link_to t('common.back'), admin_trains_path